<!-- Subject line convention: `area: what is now true`. See .github/CONTRIBUTING.md -->

## What this changes

<!-- What is true after this that was not true before, and which packages it touches. -->

## How it was tested

<!-- `zig build test`, plus what actually verifies the behaviour: which seeds, how many, against
     what captured output. For a binding, say which language you ran it from. -->

## Where the numbers came from

<!-- Only if this adds or changes offsets, table values, bit widths, opcodes or seed arithmetic:
     say what they were read from, or mark them inferred and say what would confirm them.
     Delete this section if it adds none. -->

## ABI

<!-- Only if this touches a `capi.zig` or a header: confirm `d2<pkg>_abi_version` was bumped and
     every binding's expected value updated. Delete otherwise. -->
